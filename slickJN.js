$(document).ready(function(){
  $('.slideshow').slick({
    //setting-name: setting-value
    prevArrow: $('.prev'),
      nextArrow: $('.next'),
      dots: true,
      infinite: true,
      speed: 900,
  });
});
